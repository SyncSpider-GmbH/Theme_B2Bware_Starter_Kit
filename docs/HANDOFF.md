# Project handoff

Client project notes (not the theme authoring contract — that lives in `theme/docs/`).

- Client / brand:
- Staging storefront URL:
- Admin URL:
- Who uploads the zip:
- Open questions / backlog:
